import { PreAppPermissionScreen } from '@/features/permissions/PreAppPermissionScreen'

/** Combined gallery + notifications explainer before OS prompts (Figma 698:16838). */
export default function PermissionIntroRoute() {
  return <PreAppPermissionScreen />
}
